# skills 目录说明

这个目录是“用户可读导航层”。

真正给 AI 工具运行时使用的 Skill 资产仍然放在：

- `.agents/skills/`

这样做的原因很简单：

- 不破坏现有 Codex / Kimi / Claude 的 Skill 识别方式
- 同时给用户保留一个清晰、可见、好理解的入口

## 当前 Skill

- `deterministic-rpa-workflow/`
  - 对应运行时目录：`.agents/skills/deterministic-rpa-workflow/`
