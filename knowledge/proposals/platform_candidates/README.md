# Platform Candidates

This directory stores candidate platform hardenings derived from repeated task evidence.

Entries here are suggestions only.

The user must explicitly approve any promotion into `platform/`.
